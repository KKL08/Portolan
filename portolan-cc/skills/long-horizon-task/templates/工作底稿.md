# 工作底稿：{{任务名}}

状态：准备中

## 环节与证明清单
{{发起摸底 c/d 步填；每环节一行：环节名 / eval（断言或 rubric 指针→rubric.md；人工点行写"人工核什么"）/ 裁判层级 / 节点类型（自闭环 / 人工点）}}
可自闭环比例：{{n}}/{{m}}

## 起点事实表
{{发起摸底 b 步实测填：断言 / 验证命令 / 实测结果 / 置信标（✅🟡🔴）}}
基线锚点：{{起点 commit SHA；非 git 环境记受保护文件哈希清单}}

## 编排状态
{{state-guard 独占投影（sidecar `state.json` 才是权威；本节仅人可读镜像，编辑不改变状态——下次 orch-set 会覆盖）；字段权威清单=state-guard ORCH_FIELDS；dispatch 组装时初始化为默认值 + tolerance_tier=签约选档}}

## 冻结哈希
{{state-guard update-freeze 写入；每行一条 `- <文件名>: \`<sha256_hex>\``；覆盖任务协议单.md / rubric.md（有软 eval 才有）/ execution.md（分发组装后追加）；finish 第零步核对，防执行期契约被改}}

## 运行参数
- 任务目录：{{路径}}
- 执行模型：{{执行环开工首条 journal 自报，此处留档可空；finish 以 journal 首条为准核对评审模型}}
- 试跑规模：{{小装药描述；快速通道则写"快速通道，免试跑"}}
- 熔断值：{{用户拍板值，未设则写"未设，风险自担"}}
- 轮次上限 N：{{用户拍板值，试跑后按实测校准；未设则写"未设，风险自担"}}
- 成本预估：{{试跑后按实测单轮消耗外推；未试跑则写签约估计}}

## 试跑复盘
{{三必测与按需项结果、journal 试写样条、未点火环节记录；快速通道写"快速通道，免试跑"}}

## 分发记录
- /goal 命令文本：{{组装后原文，试跑绿灯或用户跳过后填}}
- 环境准备清单：{{dispatch 组装时给出：权限白名单建议；不可逆命令绝不建议加白}}
- execution.md 哈希：{{见上方"## 冻结哈希"节；dispatch 组装/重组时调用 state-guard update-freeze 写入}}

## 修订记录
{{改什么 / 为什么 / 谁批准，一次修订一行}}
